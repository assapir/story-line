apiVersion: apps/v1
kind: Deployment
metadata:
  name: story-line-api
  labels:
    app: story-line-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: story-line-api
  template:
    metadata:
      labels:
        app: story-line-api
    spec:
      nodeSelector:
        "beta.kubernetes.io/os": linux
      containers:
        - name: story-line-api
          image: meijin007/story-line-api
          ports:
            - containerPort: 1337
          resources:
            limits:
              cpu: "900m"
              memory: "4Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: story-line-api
spec:
  ports:
  - port: 1337
  selector:
    app: story-line-api